{
  "name": "s-tier-template",
  "private": true,
  "scripts": {
    "build": "turbo run build",
    "dev": "turbo run dev",
    "lint": "turbo run lint",
    "typecheck": "turbo run check-types",
    "check": "ultracite check",
    "fix": "ultracite fix",
    "clean": "turbo clean",
    "prepare": "lefthook install",
    "deps:update": "pnpm update -r",
    "deps:mismatches": "pnpm dlx syncpack list-mismatches"
  },
  "devDependencies": {
    "@biomejs/biome": "^2.4.0",
    "lefthook": "^2.1.1",
    "turbo": "^2.8.10",
    "typescript": "catalog:",
    "ultracite": "7.2.3"
  },
  "packageManager": "pnpm@10.20.0",
  "engines": {
    "node": ">=22.0.0"
  }
}
